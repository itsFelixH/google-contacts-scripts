// ╔══════════════════════════════════════════════════════════════════════════════╗
// ║                        GOOGLE CONTACTS SCRIPTS                              ║
// ║                        Configuration Settings                               ║
// ╠══════════════════════════════════════════════════════════════════════════════╣
// ║  Copy this file to config.js and adjust the values below.                   ║
// ║  Do NOT commit config.js — it may contain personal settings.                ║
// ╚══════════════════════════════════════════════════════════════════════════════╝


// ═══════════════════════════════════════════════════════════════════════════════
// 1. FEATURES — Which reports should be available?
// ═══════════════════════════════════════════════════════════════════════════════

// ─── Email notifications ──────────────────────────────────────────────────────

const sendUnlabeledReport = true;
const sendMissingBirthdayReport = true;
const sendUpcomingBirthdaysReport = true;
const sendInvalidPhonesReport = true;
const sendMissingSurnamesReport = true;
const sendDuplicatesReport = true;
const sendStatisticsReport = true;

// ─── Contact filtering ────────────────────────────────────────────────────────

// Set useLabel to true and add label names to only report on specific contacts.
// When false, all contacts are included.
const useLabel = false;
const labelFilter = []; // e.g. ['Friends', 'Family']


// ═══════════════════════════════════════════════════════════════════════════════
// 2. REPORT SETTINGS
// ═══════════════════════════════════════════════════════════════════════════════

// ─── Upcoming birthdays ───────────────────────────────────────────────────────

const upcomingBirthdaysDays = 7; // days to look ahead

// ─── Limits ───────────────────────────────────────────────────────────────────

const maxContactsPerReport = 0; // 0 = unlimited

// ─── Links ────────────────────────────────────────────────────────────────────

const includeEditLinks = true;
const includeWhatsAppLinks = true;


// ═══════════════════════════════════════════════════════════════════════════════
// 3. SCHEDULES — When should things run?
// ═══════════════════════════════════════════════════════════════════════════════
// Used by setupSchedules(). Run it once after deploying.
// Hours are approximate — Apps Script triggers fire within a ±1 hour window.

const scheduleReportDay = ScriptApp.WeekDay.MONDAY;
const scheduleReportHour = 8;


// ═══════════════════════════════════════════════════════════════════════════════
//
//   ✅ You can stop here. Everything below is optional customization.
//
// ═══════════════════════════════════════════════════════════════════════════════


// ═══════════════════════════════════════════════════════════════════════════════
// 4. VALIDATION RULES
// ═══════════════════════════════════════════════════════════════════════════════

const minPhoneLength = 7;
const maxNameLength = 50;
const phoneRegex = /^[+]?[(]?[0-9]{1,4}[)]?[-\s./0-9]*$/;


// ═══════════════════════════════════════════════════════════════════════════════
// 5. API SETTINGS
// ═══════════════════════════════════════════════════════════════════════════════

const apiPageSize = 100;
const apiMaxRetries = 3;
const apiPersonFields = 'names,birthdays,memberships,emailAddresses,phoneNumbers,addresses,biographies';


// ═══════════════════════════════════════════════════════════════════════════════
// 6. EMAIL CUSTOMIZATION
// ═══════════════════════════════════════════════════════════════════════════════

// ─── Email subjects ───────────────────────────────────────────────────────────

const emailSubjects = {
  unlabeled: '🏷️ Contacts Without Labels 🏷️',
  missingBirthday: '🎂 Contacts Without Birthday 🎂',
  upcomingBirthdays: '🎂 Upcoming Birthdays 🎂',
  invalidPhones: '📱 Invalid Phone Numbers 📱',
  missingSurnames: '👤 Contacts Without Surnames 👤',
  duplicates: '🔍 Potential Duplicate Contacts 🔍',
  statistics: '📊 Contact Statistics 📊',
  missingField: '📋 Contacts Missing {field} 📋',
  labelUsage: '📊 Label Usage Statistics 📊',
  contactsByCity: '🌆 Contacts by City 🌆',
};


// ═══════════════════════════════════════════════════════════════════════════════
// 7. ADVANCED
// ═══════════════════════════════════════════════════════════════════════════════

const verboseLogging = false;
const dryRun = false; // true = preview mode, no emails sent
